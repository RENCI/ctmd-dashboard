apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ctmd-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "ctmd-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ctmd-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
